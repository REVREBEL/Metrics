kind = "web"
previewPath = "/"
title = "RevRebel Metrics"
version = "1.0.0"
id = "artifacts/revrebel-metrics"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22581

[services.development]
run = "pnpm --filter @workspace/revrebel-metrics run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/revrebel-metrics", "run", "build" ]
publicDir = "artifacts/revrebel-metrics/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22581"
BASE_PATH = "/"
